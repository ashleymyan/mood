Add a fourth page to the gradio demo that showcases n image interpolation. Assuming the user inputs a set of images and trains a mood space using the first gradio page, this page allows the user to select an image from the input images as the base image. Then the user can select how many clusters and match method for computing clusters, and then clusters are computed from each image to the base image. Next show an interface where each cluster is displayed across all images. For each cluster, there are sliders that determine how to weight that cluster from each image; if there are n input images, then there should be n sliders for each cluster. After the user set's all the weights, they can click on a generate button that perform n image interpolation per cluster and displays the generated image.